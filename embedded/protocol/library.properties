name=HelixProtocol
version=0.1.0
author=Helix
maintainer=Helix
sentence=Helix protocol core: packet parsing, service dispatch, and endpoint routing.
paragraph=The platform-agnostic Helix protocol/dispatcher/endpoint core, shared verbatim by the ESP32 and Arduino firmware. Depends on cJSON, FreeRTOS, and an esp_err/esp_log compat layer, which the platform provides (ESP-IDF natively; Arduino via the HelixEspCompat library).
category=Communication
url=https://github.com/helix-kit/helix-kit
architectures=*
depends=FreeRTOS
