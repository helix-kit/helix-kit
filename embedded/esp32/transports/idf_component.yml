version: "0.0.1"
description: "ESP-only Helix transports (serial/UART, MQTT, WebSocket, BLE) over the shared Helix protocol core."
url: "https://github.com/helix-kit/helix-kit"
license: "AGPL-3.0-only"
dependencies:
  idf:
    version: ">=5.4"
  espressif/esp_websocket_client:
    version: "^1.4.0"
