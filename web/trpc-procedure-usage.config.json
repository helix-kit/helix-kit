{
  "ignoreProcedures": [
    { "path": "blogPublic.countPublished", "reason": "Public blog API; server/RSS-consumed, not a frontend hook." },
    { "path": "blogPublic.publishedSlugs", "reason": "Public blog API; sitemap/RSS-consumed, not a frontend hook." },
    { "path": "devices.events", "reason": "Device events API; reusable backend surface, UI not yet wired." },
    { "path": "espFlasher.getFirmwares", "reason": "ESP32 firmware-list API; reusable backend surface." },
    { "path": "features.catalog", "reason": "Feature catalog API; reusable backend surface." },
    { "path": "features.deviceOverrides", "reason": "Per-device feature overrides API; reusable backend surface." },
    { "path": "ice.config", "reason": "ICE server config API; consumed by device/browser P2P clients, not a frontend hook." },
    { "path": "agent.getConversation", "reason": "Agent thread store; consumed server-side by the chat route caller + the agent chat UI (HELIX-157)." },
    { "path": "agent.listConversations", "reason": "Agent thread store; consumed by the agent chat UI (HELIX-157)." },
    { "path": "agent.createConversation", "reason": "Agent thread store; consumed by the agent chat UI (HELIX-157)." },
    { "path": "agent.renameConversation", "reason": "Agent thread store; consumed by the agent chat UI (HELIX-157)." },
    { "path": "agent.deleteConversation", "reason": "Agent thread store; consumed by the agent chat UI (HELIX-157)." }
  ],
  "ignoreUsagePaths": ["reports/**"],
  "manualUsages": [],
  "reportJsonPath": "../reports/trpc-procedure-usage.json",
  "reportMarkdownPath": "../reports/trpc-procedure-usage.md",
  "rootScanRoots": ["apps"],
  "usageRoots": ["apps", "packages", "../tests/e2e", "../tooling", "../cloud"]
}
