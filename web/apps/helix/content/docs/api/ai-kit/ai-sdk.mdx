---
title: 'ai-sdk'
description: 'Public API of @helix-hq/ai-kit/ai-sdk.'
---

The complete public surface of `@helix-hq/ai-kit/ai-sdk`, generated from the published type
declarations. Anything absent here is not part of the package's contract.

```ts
import { JSONSchema } from 'zod/v4/core';
import { ToolSet } from 'ai';

// @public (undocumented)
export type ToolSetOptions = {
    onCall?: (call: {
        name: string;
        toolCallId: string;
        input: unknown;
        output: unknown;
        error: string | null;
        durationMs: number;
    }) => Promise<void> | void;
};

// @public
export const toToolSet: (descriptors: AiToolDescriptor[], opts?: ToolSetOptions) => ToolSet;

// (No @packageDocumentation comment for this package)
```
