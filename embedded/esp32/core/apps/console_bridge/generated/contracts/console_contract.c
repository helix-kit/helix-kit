/* Code generated by `uv run helix protocol generate --target embedded-c`. DO NOT EDIT. */
#include "console_contract.h"

#include "helix_json.h"

static const helix_json_field_t s_open_session_request_fields[] = {
    HELIX_JSON_FIELD(console_open_session_request_t, session_id, "sessionId", HELIX_JSON_FIELD_STRING, true),
    HELIX_JSON_FIELD(console_open_session_request_t, transport, "transport", HELIX_JSON_FIELD_STRING, false),
    HELIX_JSON_FIELD(console_open_session_request_t, data_url, "dataUrl", HELIX_JSON_FIELD_STRING, false),
    HELIX_JSON_FIELD(console_open_session_request_t, token, "token", HELIX_JSON_FIELD_STRING, false),
};

esp_err_t console_parse_open_session_request(const cJSON *payload, console_open_session_request_t *out)
{
    return helix_json_decode_object(
        payload,
        out,
        sizeof(*out),
        s_open_session_request_fields,
        HELIX_ARRAY_SIZE(s_open_session_request_fields)
    );
}

static const helix_json_field_t s_close_request_fields[] = {
    HELIX_JSON_FIELD(console_close_request_t, session_id, "sessionId", HELIX_JSON_FIELD_STRING, true),
};

esp_err_t console_parse_close_request(const cJSON *payload, console_close_request_t *out)
{
    return helix_json_decode_object(
        payload,
        out,
        sizeof(*out),
        s_close_request_fields,
        HELIX_ARRAY_SIZE(s_close_request_fields)
    );
}

static const helix_json_field_t s_configure_request_fields[] = {
    HELIX_JSON_FIELD(console_configure_request_t, uart, "uart", HELIX_JSON_FIELD_INT, false),
    HELIX_JSON_FIELD(console_configure_request_t, baud, "baud", HELIX_JSON_FIELD_INT, false),
    HELIX_JSON_FIELD(console_configure_request_t, tx_pin, "txPin", HELIX_JSON_FIELD_INT, false),
    HELIX_JSON_FIELD(console_configure_request_t, rx_pin, "rxPin", HELIX_JSON_FIELD_INT, false),
};

esp_err_t console_parse_configure_request(const cJSON *payload, console_configure_request_t *out)
{
    return helix_json_decode_object(
        payload,
        out,
        sizeof(*out),
        s_configure_request_fields,
        HELIX_ARRAY_SIZE(s_configure_request_fields)
    );
}

static const helix_json_field_t s_session_payload_fields[] = {
    HELIX_JSON_FIELD(console_session_payload_t, session_id, "sessionId", HELIX_JSON_FIELD_STRING, true),
    HELIX_JSON_FIELD(console_session_payload_t, offer, "offer", HELIX_JSON_FIELD_STRING, false),
};

esp_err_t console_parse_session_payload(const cJSON *payload, console_session_payload_t *out)
{
    return helix_json_decode_object(
        payload,
        out,
        sizeof(*out),
        s_session_payload_fields,
        HELIX_ARRAY_SIZE(s_session_payload_fields)
    );
}

static const helix_json_field_t s_session_info_fields[] = {
    HELIX_JSON_FIELD(console_session_info_t, session_id, "sessionId", HELIX_JSON_FIELD_STRING, true),
    HELIX_JSON_FIELD(console_session_info_t, created_at, "createdAt", HELIX_JSON_FIELD_INT, true),
    HELIX_JSON_FIELD(console_session_info_t, terminals, "terminals", HELIX_JSON_FIELD_INT, true),
};

esp_err_t console_parse_session_info(const cJSON *payload, console_session_info_t *out)
{
    return helix_json_decode_object(
        payload,
        out,
        sizeof(*out),
        s_session_info_fields,
        HELIX_ARRAY_SIZE(s_session_info_fields)
    );
}

static const helix_json_field_t s_sessions_payload_fields[] = {
    HELIX_JSON_FIELD(console_sessions_payload_t, sessions, "sessions", HELIX_JSON_FIELD_VALUE, true),
};

esp_err_t console_parse_sessions_payload(const cJSON *payload, console_sessions_payload_t *out)
{
    return helix_json_decode_object(
        payload,
        out,
        sizeof(*out),
        s_sessions_payload_fields,
        HELIX_ARRAY_SIZE(s_sessions_payload_fields)
    );
}

static const helix_json_field_t s_config_payload_fields[] = {
    HELIX_JSON_FIELD(console_config_payload_t, ok, "ok", HELIX_JSON_FIELD_BOOL, true),
    HELIX_JSON_FIELD(console_config_payload_t, uart, "uart", HELIX_JSON_FIELD_INT, true),
    HELIX_JSON_FIELD(console_config_payload_t, baud, "baud", HELIX_JSON_FIELD_INT, true),
    HELIX_JSON_FIELD(console_config_payload_t, tx_pin, "txPin", HELIX_JSON_FIELD_INT, true),
    HELIX_JSON_FIELD(console_config_payload_t, rx_pin, "rxPin", HELIX_JSON_FIELD_INT, true),
};

esp_err_t console_parse_config_payload(const cJSON *payload, console_config_payload_t *out)
{
    return helix_json_decode_object(
        payload,
        out,
        sizeof(*out),
        s_config_payload_fields,
        HELIX_ARRAY_SIZE(s_config_payload_fields)
    );
}

static const helix_json_field_t s_error_payload_fields[] = {
    HELIX_JSON_FIELD(console_error_payload_t, error, "error", HELIX_JSON_FIELD_STRING, true),
};

esp_err_t console_parse_error_payload(const cJSON *payload, console_error_payload_t *out)
{
    return helix_json_decode_object(
        payload,
        out,
        sizeof(*out),
        s_error_payload_fields,
        HELIX_ARRAY_SIZE(s_error_payload_fields)
    );
}
