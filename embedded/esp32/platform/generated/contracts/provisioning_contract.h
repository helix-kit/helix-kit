/* Code generated by `uv run helix protocol generate --target embedded-c`. DO NOT EDIT. */
#ifndef GENERATED_CONTRACTS_PROVISIONING_H
#define GENERATED_CONTRACTS_PROVISIONING_H

#include <stdbool.h>

#include "cJSON.h"
#include "esp_err.h"

#define PROVISIONING_SERVICE "provisioning"
#define PROVISIONING_DESCRIBE_CONFIG_METHOD "describe-config"
#define PROVISIONING_GET_CONFIG_METHOD "get-config"
#define PROVISIONING_SET_CONFIG_METHOD "set-config"
#define PROVISIONING_CONFIG_MESSAGE "provisioning-config"
#define PROVISIONING_STATUS_MESSAGE "provisioning-status"
#define PROVISIONING_ERROR_MESSAGE "provisioning-error"

typedef struct provisioning_describe_config_request_t {
} provisioning_describe_config_request_t;

typedef struct provisioning_get_config_request_t {
    char * domain;
} provisioning_get_config_request_t;

typedef struct provisioning_set_config_request_t {
    char * domain;
    bool force;
    const cJSON * values;
} provisioning_set_config_request_t;

typedef struct provisioning_config_payload_t {
    const cJSON * domains;
} provisioning_config_payload_t;

typedef struct provisioning_status_payload_t {
    char * status;
    char * message;
    bool restart_required;
} provisioning_status_payload_t;

typedef struct provisioning_error_payload_t {
    char * error;
} provisioning_error_payload_t;

esp_err_t provisioning_parse_describe_config_request(const cJSON *payload, provisioning_describe_config_request_t *out);
esp_err_t provisioning_parse_get_config_request(const cJSON *payload, provisioning_get_config_request_t *out);
esp_err_t provisioning_parse_set_config_request(const cJSON *payload, provisioning_set_config_request_t *out);
esp_err_t provisioning_parse_config_payload(const cJSON *payload, provisioning_config_payload_t *out);
esp_err_t provisioning_parse_status_payload(const cJSON *payload, provisioning_status_payload_t *out);
esp_err_t provisioning_parse_error_payload(const cJSON *payload, provisioning_error_payload_t *out);

#endif /* GENERATED_CONTRACTS_PROVISIONING_H */
