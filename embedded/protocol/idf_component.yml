version: "0.0.1"
description: "Helix protocol core: packet parsing, service dispatch, and endpoint routing (platform-agnostic; shared by the ESP32 and Arduino firmware)."
url: "https://github.com/helix-kit/helix-kit"
license: "AGPL-3.0-only"
dependencies:
  idf:
    version: ">=5.4"
