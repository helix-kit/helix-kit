# SPDX-License-Identifier: AGPL-3.0-only
# passwd/group resolve through SSSD; shadow deliberately does not, because this
# directory publishes no passwords and authentication is out of scope.
passwd:         files sss
group:          files sss
shadow:         files
gshadow:        files

hosts:          files dns
networks:       files
protocols:      db files
services:       db files
ethers:         db files
rpc:            db files
netgroup:       nis
