---
title: 'editor'
description: 'Public API of @helix-hq/json-schema/editor.'
---

The complete public surface of `@helix-hq/json-schema/editor`, generated from the published type
declarations. Anything absent here is not part of the package's contract.

```ts
import { JSONSchema } from 'zod/v4/core';
import { JSX } from 'react';

// @public
export const JsonEditor: (input: {
    className?: string;
    path: string;
    schema?: JsonEditorSchema;
    theme: 'light' | 'vs-dark';
    value: string;
    onChange: (value: string) => void;
}) => JSX.Element;

// @public
export type JsonEditorSchema = {
    fileMatch: string;
    schema: object;
};

// @public
export const SchemaEditor: (input: SchemaEditorProps) => JSX.Element;

// @public (undocumented)
export type SchemaEditorProps = {
    value: JSONSchema._JSONSchema;
    onChange: (schema: JSONSchema.JSONSchema) => void;
    path: string;
    theme: 'light' | 'vs-dark';
    className?: string;
};

// (No @packageDocumentation comment for this package)
```
