/* Code generated by `uv run helix protocol generate --target embedded-c`. DO NOT EDIT. */
#ifndef GENERATED_CONTRACTS_OTA_H
#define GENERATED_CONTRACTS_OTA_H

#include <stdbool.h>

#include "cJSON.h"
#include "esp_err.h"

#define OTA_SERVICE "ota"
#define OTA_OTA_UPDATE_METHOD "ota-update"
#define OTA_STATUS_MESSAGE "ota-status"
#define OTA_ERROR_MESSAGE "ota-error"

typedef struct ota_ota_update_request_t {
    char * firmware_url;
    char * device_path;
    char * package_path;
    char * version;
    char * sha256;
} ota_ota_update_request_t;

typedef struct ota_ota_status_payload_t {
    char * state;
    char * firmware_version;
    char * target_version;
    char * sha256;
    char * message;
} ota_ota_status_payload_t;

typedef struct ota_error_payload_t {
    char * error;
} ota_error_payload_t;

esp_err_t ota_parse_ota_update_request(const cJSON *payload, ota_ota_update_request_t *out);
esp_err_t ota_parse_ota_status_payload(const cJSON *payload, ota_ota_status_payload_t *out);
esp_err_t ota_parse_error_payload(const cJSON *payload, ota_error_payload_t *out);

#endif /* GENERATED_CONTRACTS_OTA_H */
