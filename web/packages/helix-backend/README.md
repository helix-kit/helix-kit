# @helix-hq/backend

Composable backend building blocks for Helix server runtimes.

The package intentionally exposes small subpath imports so consumers can build
their own backend process without adopting the first-party `helix-server` app.
